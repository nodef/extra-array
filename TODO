# FUNCTION
- ifte(inp, cnd, then, else)
(i => i>10? i:10)(i)

not
and
or

- object.from(keys, values, deep=false) // zipObject(Deep)
- object.fromentries
- array.symmetricDifference
- array.difference
- array.union
- array.intersection
